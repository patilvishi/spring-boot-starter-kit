server:
  port: 8080

spring:
  application:
    name: spring-boot-starter-kit

  datasource:
    url: jdbc:postgresql://localhost:5432/starterkit
    username: postgres
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  
  jwt:
  secret: **********
  expiration: 86400000
